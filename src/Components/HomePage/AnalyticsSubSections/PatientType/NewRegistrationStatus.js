import React from 'react'

const NewRegistrationStatus = (props) => {
    return (
        <div className={props.baseStyle}>
            new reg
        </div>
    )
}

export default NewRegistrationStatus
